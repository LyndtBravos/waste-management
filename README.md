# waste-management
A full-stack waste management analytics application built with Spring Boot (Java) and Angular. The system allows authenticated users to view waste collection analytics through interactive charts and dashboard metrics.
